<h1>投稿詳細</h1>
<p><%= @post.song_title %></p>
<h2>コード譜</h2>
<h3>capo</h3>
  <p><%= @post.capotast %></p>

  <br>
  <% @post.scores.each do |score| %>
    <% if score.chord? %>
  <p><%= score.content %></p>
    <% elsif score.lyric? %>
  <p><%= score.content %></p>
    <% end %>
  <% end %>
  </br>
<%= render 'crud_menus', post: @post if current_user.own?(@post) %>